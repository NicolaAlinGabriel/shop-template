import { View, Text } from 'react-native';
import React from 'react';
import { DefaultText, ScreenContainer } from '@components/styles';

const Drawer2 = () => {
  return (
    <ScreenContainer>
      <DefaultText>DRAW 2</DefaultText>
    </ScreenContainer>
  );
};

export default Drawer2;
